export default function SectionPlaceholder({ title }) {
  return (
    <article className="section-placeholder glass-panel">
      <h3>{title}</h3>
      <p>This wing of the academy is still booting. Return to Quiz Arena to continue active debugging drills.</p>
    </article>
  )
}
